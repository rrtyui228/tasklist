import React from 'react';
import {Header} from 'shared';
import Modal from './Modal';

const TasksList = () => {
  return (
    <>
      <Header />
      <Modal />
    </>
  );
};

export default TasksList;
